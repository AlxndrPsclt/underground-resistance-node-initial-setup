all:
  children:
    rpis:
      hosts:
        newraspberrypi:
        underground-resistance-001:
      vars:
        ansible_connection: ssh
        ansible_user: pi
        ansible_ssh_pass: raspberry
        ansible_port: 22
